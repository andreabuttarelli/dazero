import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-20',
  title: 'The brand chat finishes what it starts',
  items: [
    'The assistant can now do far more in a single message, so a request that takes many steps — writing several things, then arranging them — runs to the end instead of stopping halfway.',
    'When a long piece of work runs out of time, the assistant wraps up and saves what it did, rather than cutting off mid-sentence and losing it.'
  ]
} satisfies ChangelogEntry;
