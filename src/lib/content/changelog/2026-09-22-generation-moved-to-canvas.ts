import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-22',
  title: 'Image and video generation now happens on the canvas',
  items: [
    'The standalone image, video, carousel and refine tools have been removed — drop a node on the canvas and generate there instead.'
  ]
} satisfies ChangelogEntry;
