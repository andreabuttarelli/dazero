import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-24',
  title: 'Resize any node from its edges and corners',
  items: ['Select a node and drag its edges or corners to resize it — the new size is saved.']
} satisfies ChangelogEntry;
