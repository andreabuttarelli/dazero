import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-24',
  title: 'Read generated text as Markdown or raw',
  items: [
    'A text node’s output now renders as formatted Markdown by default, with a toggle to see the raw text instead — your choice is remembered per node.'
  ]
} satisfies ChangelogEntry;
