import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-21',
  title: 'Documents on the canvas',
  items: [
    'A new Document tile on the board: write markdown and read it rendered, like a note that stays where you put it.',
    'Any document can get a public link — share it with anyone, revoke it when you want. Expired, revoked and unknown links all answer the same way: not found.'
  ]
} satisfies ChangelogEntry;
