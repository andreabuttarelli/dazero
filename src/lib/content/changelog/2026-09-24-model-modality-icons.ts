import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-24',
  title: 'See what each model can read',
  items: [
    'The model picker now shows small icons for what each model can read — text, image, video, audio or file.'
  ]
} satisfies ChangelogEntry;
