import type { ChangelogEntry } from './index';

export default {
  date: '2026-09-24',
  title: 'Run connected nodes as a workflow',
  items: [
    'Run connected nodes as a workflow with the Play button; it keeps going with the tab closed.'
  ]
} satisfies ChangelogEntry;
